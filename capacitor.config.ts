import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'gringo.directory',
  appName: 'Gringo Directory',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
